import {Text, View} from "react-native";

function CreateNewGroup({navigation}) {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Create New Group Screen</Text>
        </View>
    )
}

export default CreateNewGroup